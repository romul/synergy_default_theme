<%
   image ||= @product.images.first
   if image %>
  <%= link_to image_tag(image.attachment.url(:product), :alt => @product.name), image.attachment.url(:large), :rel => '#large_image_overlay' %>
<% else %>
  <%= product_image(@product) %>
<% end %>

